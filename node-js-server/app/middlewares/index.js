const authJwt = require("./authJwt");
const verifySignUp = require("./verifySignUp");

/* Exporting the functions from the authJwt and verifySignUp files. */
module.exports = {
  authJwt,
  verifySignUp
};
